import { MD3LightTheme as DefaultTheme } from 'react-native-paper'

export const projectTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors
    // primary: 'tomato',
    // secondary: 'yellow'
  }
}
